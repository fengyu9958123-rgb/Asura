"""
Shared prompt sections for testcase writer agents.
"""

COMMON_FACT_RULES = """# 通用事实规则

- “PRD 原文”BLOCK 是唯一需求事实来源。
- “当前 LU”定义本次生成范围；标题、摘要、全局需求认知只用于理解背景和范围，不得作为新增需求事实。
- “辅助证据 BLOCK”只能补充字段、枚举、提示文案、状态、边界或流程上下文，不能单独驱动用例。
- “heading_path”和“lines”只用于定位原文，不是需求事实。
- 可以发散测试场景，但只能基于 PRD 已声明事实；不要新增文档未声明的页面、角色、按钮、字段、状态、异常、阈值、排序、权限、实现方式或业务能力。
- 如果 PRD 给出了精度、单位、可选值、数量范围、时间范围或枚举列表，所有测试数据和边界值都必须服从这些定义，不得升级或细化粒度。
- 可以准备必要测试数据，但前置条件不能引入 PRD 未声明的页面、权限、排序、状态流转或系统行为。
- 用例类型由你根据测试价值自行命名，不限定枚举；非功能类用例只有在 PRD 明确声明对应要求时才生成。
"""

NORMAL_BEHAVIOR_RULES = """# 普通 LU 写作侧重点

- 普通 LU 以主需求 BLOCK 的完整覆盖为目标，优先把单个功能、规则、字段、状态、异常和边界写透。
- 重要或高风险内容优先补充单点主路径、边界、异常、组合和局部联动。
- 重复控制更关注同一对象、同一条件、同一步骤的重复验证，不要把等价用例拆太碎。
- 优先级更偏向单点功能的业务影响和规则影响。
"""

INTEGRATION_BEHAVIOR_RULES = """# 链路 LU 写作侧重点

- 链路 LU 以 flow_hint 和 flow_outline 为中心，优先覆盖流程穿越、状态传递、数据一致性、操作对象联动和异常收尾。
- 重要或高风险内容优先补充跨 LU 闭环、上下文传递、刷新/清空后的残留、一致性校验和链路异常。
- 重复控制更关注是否在重复写单点功能；如果只是换了一个入口或对象但链路关系没变，应该合并。
- 优先级更偏向链路中断、数据错位、状态污染、闭环失败和关键联动失败。
"""
NORMAL_DUPLICATE_RULES = """# 普通 LU 重复控制补充

- 普通 LU 可以保留对象不同、边界不同、断言不同的相似用例。
- 同一功能点的多种输入、多个状态、多个边界值得单点验证可以保留。
"""

INTEGRATION_DUPLICATE_RULES = """# 链路 LU 重复控制补充

- 链路 LU 只保留能体现链路变化的用例，不要把同一链路拆成多个只差一个单点步骤的用例。
- 如果只是单个字段、按钮或枚举在重复验证，而没有体现跨 LU 的状态或结果变化，应合并。
"""

NORMAL_PRIORITY_RULES = """# 普通 LU 优先级补充

- P0 更偏向单点核心功能被阻断、规则失效、数据错误或强限制未生效。
- P1 更偏向主功能路径、关键规则和重要展示。
- P2/P3 更偏向普通默认值、普通展示和低频辅助验证。
"""

INTEGRATION_PRIORITY_RULES = """# 链路 LU 优先级补充

- P0 更偏向跨 LU 链路中断、状态传递错误、数据不一致、关键闭环失败或清理失败。
- P1 更偏向主要链路路径、关键联动和重要数据一致性。
- P2/P3 更偏向普通链路观察点和低频辅助验证。
"""

COMMON_OUTPUT_RULES = """# 输出格式

只输出 Markdown 表格和完成标记，不输出解释、策略说明、覆盖说明、代码块围栏、`<think>` 或 `<analysis>`。

表头严格为：
| 功能模块 | 测试场景分类 | 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 用例类型 |

格式要求：
- 第一行必须直接输出表头，不要在表格前输出任何文字。
- 必须包含 Markdown 表头分隔行，且表头、分隔行、每条用例都必须是 9 列。
- 只能输出上述 9 列，不要新增“关联需求”“测试包ID”“测试包类型”“测试ID”“说明”“覆盖点”等额外列。
- 每条用例必须单独占一行，整行必须以 `|` 开头并以 `|` 结尾。
- 用例编号使用 001、002、003...；后端会按局部单元重编号。
- 表格单元格内只能用 `<br>` 换行，严禁普通换行、项目符号列表、代码块或嵌套表格。
- 表格单元格内不要出现半角竖线 `|`；如果 PRD 原文含有 `|`，请改写为不影响事实的中文分隔表达。
- 前置条件、测试步骤、预期结果使用编号格式：`1.内容一<br>2.内容二`。
- 优先级只能使用 P0/P1/P2/P3。
- 如果文档给出精确文案、提示语、字段名或数量限制，步骤和预期结果必须使用原文，不要写“类似”“等”。

输出前确认：覆盖完整、未偏离 PRD 事实、无低价值重复、表格格式可解析。

表格后输出：【测试包用例生成完成】
"""
